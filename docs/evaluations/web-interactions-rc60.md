# Web 交互回归 · 0.72.0-rc60

日期：2026-09-08

## 需求验收

| 需求 | 验证与结果 |
| --- | --- |
| 插件中心位于左上插件之后 | 浏览器加载真实构建产物，导航顺序为对话、收藏、一句话待办、插件中心；插件中心不再靠底部。 |
| 语言与主题由图标触发 | 默认只显示设置图标；点击后出现两项选择器，切换深色主题生效，Escape 收起面板。 |
| 隐藏用户气泡中的自动上下文 | 隔离的 rc60 Runtime 加载包含完整附件协议的磁盘会话，实际 Web 页面只显示分隔符后的用户正文；正常助手回复仍显示。 |
| 选区插件菜单不再闪退 | 浏览器拖选后右键，菜单跨后续状态读取保持可见；分别点击收藏与一句话待办，两次命令均携带完整选区文字与 chat.selection 来源。 |

## 自动检查

- `cargo test -p willdeep-core --lib conversation:: --offline`：11 通过，包括完整协议、CRLF、空正文、转义边界、残缺协议与普通引用保留。
- `yarn --cwd web lint`：通过。
- `yarn --cwd web build`：通过；存在 Vite 大于 500 kB 的包体积提示。
- `cargo build -p willdeep --offline`：通过。
- `target/debug/willdeep --version`：0.72.0-rc60。
- `git diff --check`：通过。

## 验证范围

浏览器插件菜单使用合成插件和隔离 API 验证宿主交互、菜单生命周期及命令参数，没有修改真实插件数据。自动上下文隐藏另用真实 Runtime、独立 WILLDEEP_HOME 和独立工作区验证。原始模型消息不变，仅改变展示投影；残缺协议不猜测删除。未安装、提交或发布本版本。
