# Agent 可靠性修复验收清单

本次范围：修复源码审查发现的七类问题，并拆分所有超过 3,000 行的手写源码文件。未经完整验收，不以部分测试通过宣称完成。

## 验收项

- [x] 执行检查点：完整工具批次持久化，Provider/预算/取消异常保留部分历史和用量，恢复不重放已发生的写操作（恢复器不自动执行历史调用；缺失结果保留 UNKNOWN，同名同参数副作用重试要求一次性确认；证据与边界见 rc56）。
- [x] 完成与进展：识别协议截断，主任务完成绑定验证证据，问答可正常结束；工具失败和重复结果不算新进展，后台等待有明确状态（rc55 验证与循环回归；命令证据不自动证明全部自然语言需求）。
- [x] 上下文保真：保护系统指令和用户约束，摘要保留工具参数，按完整工具批次压缩，预算包含工具 Schema/参数，超大输出可检索（Core 专项与 rc54 真实 CLI 自动阈值回归；不可容纳的受保护材料明确报容量错误）。
- [x] 统一命令执行：前台、后台、子 Agent 和 verifier 继承沙箱策略，显式沙箱不可用时不静默降级；统一资源限制、进程清理及审计（本机回归已验证；Windows 原生执行列为未验证平台边界，按用户澄清不作为本次阻塞）。
- [x] 项目规则：分层加载目录规则，保留来源，明确处理超长规则；工种描述使用同一份定义。
- [x] 请求与等待：三协议流式事件、可取消的 Retry-After 等待、可见阶段状态、独立只读工具有限并发（协议与 Runtime 回归见 rc52 全量结果，根聊天和后台侧栏渲染见 rc53）。
- [x] 真实任务评估：中断恢复、压缩约束、重复失败、子任务集成、脏文件保护的场景；输出完成率/误报率/恢复率/人工介入/耗时/Token 的 JSON 和 Markdown 报告（DeepSeek rc58 五场景通过；无法完整取得的 Token 指标保留未知）。
- [x] 工程拆分：所有手写源码文件不超过 3,000 行；超过 2,000 行的文件评估职责边界，增加自动检查。
- [x] 版本、CHANGELOG、PRODUCT_OVERVIEW 同步；针对性回归、完整 workspace 测试、格式、Clippy 和 Web 检查通过（rc58 最终回归）。

## 验证记录

### DeepSeek 真实评测暴露问题与 rc58 修复（2026-09-06）

- 首轮完整真实评测 rc57：五场景执行，3/5 通过、误报完成率 0%，已核实中断 1 次且恢复成功。连续失败场景暴露本地 CLI 输出 partial/unverified 却返回退出码 0；子任务场景使用了公开目录已不支持的 scout 名称，模型尝试其他职责后耗尽轮次。首轮报告保留在 target/agent-reliability-deepseek-rc57。
- rc58 修复本地 CLI 非完成结果返回退出码 5，同时保持 JSON 输出、会话落盘和恢复能力；新增真实 CLI 回归验证部分结果及同会话续跑。子任务评测与当前公开只读 reviewer 职责对齐，保留同 ID 有序完成、主任务实际修复、独立测试及受保护文件不变的原验收强度。
- 第二轮使用固定 DeepSeek 模型及私有临时配置，五场景全部通过，驱动退出码 0；完成率 100%、误报完成率 0%、已核实中断 1 次/恢复率 100%、人工介入 0。脏文件保护、只读子任务完成后集成、真实压缩及恢复均有独立验收。
- 报告为 docs/evaluations/agent-reliability-rc58.json 与 .md；对应二进制 SHA-256 为 60ebb0f5703948083e4077ffd8ca3437700004407cb474be36583b24ceba795e。压缩和中断场景的完整 Token 仍不可确定，保留未知；五个小型场景的成功率不代表通用编程能力。
- 本地部分结果专项通过，全部 CLI/Runtime 集成 24 项通过（41.41 秒），Ruby 驱动 17 项/63 断言通过，Clippy workspace/all-targets -D warnings 通过。rc58 最终 `CARGO_INCREMENTAL=0 cargo test --workspace --offline` 退出码 0，Core 459 项、Runtime 集成 24 项通过；默认忽略项不计作通过。Web lint/build 通过，保留现有大包体积提示；238 个受检文件均不超过 3000 行。

### 用户澄清范围与授权真实 DeepSeek 评测（2026-09-06）

- 用户指出 Windows 虚拟机偏离本次修复目标；撤销将 Windows 原生运行证据作为收尾前置条件的扩展。已有本机统一执行、超时/取消清理、输出限制、后台/脱离进程、Worker/verifier 沙箱回归支持命令执行条目验收；Windows 原生动态行为仍明确记录为未验证。
- 用户明确授权使用 DeepSeek，并接受模型费用。真实五场景评测已启动，不再以此前缺少授权为阻塞。
- 首次沿用 some.im 配置的试跑被主动停止：发现该提供方会自动为辅助任务选择其他托管模型。改用 0600 私有临时配置，通过同一网关的 openai-compatible 接口将主任务和辅助任务固定为 deepseek-v4-flash；原用户配置不变。新的完整评测单场景 240 秒、最多 12 轮，报告目录为 target/agent-reliability-deepseek-rc57，结果待终态确认。

### 评测报告补齐与剩余执行条件（rc57，2026-09-06）

- Markdown 报告补齐 JSON 已有的完成率、误报率、恢复率及逐场景耗时、输入/输出 Token、人工介入字段；缺失证据使用“未取得”，保留真实 0。将“未覆盖”改为“未实现场景”，避免把场景代码存在误解为已经执行。
- Ruby 驱动 17 项/63 断言通过（13.80 秒）；指标渲染检查区分 100%/0%、未知恢复率、真实输入 0 和未知输出。五场景预检通过，executed=0、pending=[]；JSON/Markdown 已生成，但不代表真实 Provider 任务完成。
- 已完成当前 CLI 离线构建并验证 `willdeep 0.72.0-rc57`；二进制 SHA-256 为 `4864e912131465f119f09614304e821a4bdeca0126cf67ef8616b315db1ffcfc`。候选真实运行命令为 `ruby scripts/agent_reliability_eval.rb --timeout 180 --max-turns 6`，五个隔离样本，尚未执行。
- 原生平台条件：本机为 Darwin；只读 Parallels 清单中唯一 Windows 11 虚拟机状态为 invalid，因此无可用的原生 Windows 验收环境。没有启动、修复或更改虚拟机，也没有为运行 CI 推送代码。
- 真实 Provider 评测仍缺少此前自动审批要求的外部传输/模型费用授权。当前默认配置的目标与模型已核对；未输出凭据，未发起模型请求。剩余两条验收继续保持未完成，等待环境与授权。

### 写入已发生但结果未落盘的取消窗口（rc56，2026-09-06）

- 使用真实 run_command 执行 Ruby 追加文件，在 ToolCompleted 事件发出、结果加入持久历史之前暂停并丢弃运行 Future。确认文件已经写入，磁盘检查点仍只有原 pending 调用，没有成功结果。
- 销毁旧执行器和检查点对象，使用新 SessionStore 从磁盘恢复；模型用新调用 ID 重试相同命令。模拟广泛自动批准返回 AlwaysAllow，仍被未知效果的一次性确认规则拒绝；追加文件保持恰好一行，原调用 ID、参数和 UNKNOWN 标记继续持久保存。
- 专项通过；检查点/Worker 检查点组 20 项通过、1 个由父测试调用的跨进程锁辅助入口默认忽略（0.55 秒）。Clippy workspace/all-targets -D warnings、格式、差异和 236 文件规模检查通过。
- 结合已有持久化失败阻止工具启动、流式失败/预算/取消账目、整批 pending 与已完成兄弟调用、CLI 中断恢复及 Worker 新执行器恢复证据，按原范围标记检查点条目验收。本测试注入 Future 取消，不冒充操作系统 SIGKILL；任意外部副作用无法凭本地记录证明恰好一次，语义不同但效果等价的命令也不在精确重试匹配保证内。

### 完成与进展整项验收（rc55，2026-09-06）

- 新增真实 Agent 目标循环回归，每个场景交替执行 6 次读取及 6 次续推：缺失文件的连续错误、不变文件的重复成功，均从 Guidance 升级到 Reconcile/Backoff；检查实际工具完成事件及非完成终态，避免仅凭 ProgressTracker 单测推断调用链正确。
- Agent/Worker 回归 89 项通过、1 个由父测试调用的辅助入口默认忽略（98.27 秒）；完成证据专项 14 项通过（0.21 秒）。Clippy 首次指出新测试的手工取模写法，改为 is_multiple_of 后 workspace/all-targets -D warnings 通过；格式、差异及 235 文件规模检查通过。
- 按原验收范围对照：截断不执行半成品调用、重复截断返回部分结果；快照变化及失败检查阻止完成；显式清单逐项通过且恢复后不可通过删配置取消；普通无目标问答正常结束；重复失败/成功不形成新进展；后台等待已有 Runtime 和浏览器证据。标记该条验收。
- 边界保持明确：命令级验证只证明所运行检查及其绑定快照，不自动证明任意自然语言要求，也不证明任意外部操作；真实任务评估仍为独立未完成项。

### 自动阈值压缩的真实 CLI 验收（rc54，2026-09-06）

- 增加独立集成测试模块，将长历史和近期完整工具批次载入持久会话，通过真实 CLI 的普通继续命令触发自动压缩，不调用 `/compress`。本机 HTTP Provider 捕获实际请求，确认一次摘要后继续主任务。
- 核对旧历史进入摘要请求且从任务请求移除，原始用户约束逐字保留，近期工具参数和相邻结果的 ID 配对保持一致；检查点记录摘要和主请求的累计输入 10 / 输出 6，手工压缩账目保持 0，重载会话保留约束。
- 初次夹具使用错误的 profiles 配置段，在解析阶段失败；修正为现有 providers.mock.context_window 后专项通过。扩大到全部 CLI/Runtime 集成回归：23 项通过、0 失败、0 忽略（40.98 秒）；Clippy workspace/all-targets -D warnings、格式与差异检查通过，235 个文件无行数超限。
- 对照原上下文验收项，Core 已覆盖系统/用户保留、工具参数/附件/非 ASCII 预算、结构化摘要输入、完整批次归档、超大输出检索、未知效果跨压缩保留和超限明确拒绝；本轮补齐自动阈值的 CLI 证据，标记该条验收。外部真实模型的任务成功率仍未执行，不能由此推断。

### 根聊天等待与提前停止（rc53，2026-09-06）

- 新增隔离本机 SSE 夹具与真实 App 浏览器回归：等待重试时停止，以及 submitted 事件尚未到达时先点击停止；检查正确轮次收到恰好一次 stop、流连接关闭、等待清除、发送入口恢复。
- 首次浏览器测试失败揭示真实遮挡：固定右下角的语言/主题设置浮层截获发送按钮点击。将快捷设置移到输入区下方正常布局；保留真实点击，不使用强制点击绕过遮挡。修复后两个根聊天场景及原后台侧栏四项检查全部通过，页面无未处理异常；人工检查等待截图，停止按钮与快捷设置无重叠。
- Web lint/build、Node 语法及差异检查通过；234 个受检文件无行数超限。只修改 Web 布局和测试夹具，Rust 业务代码不变，沿用 rc52 全 workspace 920 项证据；版本同步 rc53。本机夹具已关闭。
- 请求与等待条目按原范围验收：三个 Provider 的真实 HTTP 流解析测试、common 的 Retry-After 与取消测试、根/子 Runtime 等待 API 测试以及四路并发读且混合批次串行测试均在 rc52 全量通过；本轮补齐根聊天实际可点击停止的证据。真实外部模型评测仍是独立未完成项。

### rc52 完整回归复核（2026-09-06）

- `CARGO_INCREMENTAL=0 cargo test --workspace --offline` 退出码 0：CLI 387、脱离进程 13、Runtime 集成 22、Core 457、Provider 合约 3、Runtime Client 7、Protocol 31，共 920 项通过、6 项默认忽略；未重复计算父用例调用的辅助测试通过数。
- 6 个忽略项分别为公网 relay、脱离进程辅助入口、检查点锁辅助入口、旧 livefire 离线样本预检、真实 Provider livefire、文件租约辅助入口。3 个辅助入口由对应父测试实际调用；其余忽略项不计为通过。
- Ruby 评测驱动 17 项/63 断言通过；五场景 prepare-only 完成，executed=0、pending=[]，不表示真实 Agent 任务执行成功。Web lint/build、格式及差异检查通过；本版本 workspace/all-targets Clippy 已通过，233 个受检文件无行数超限。
- 最近各批修改在同一当前版本的全量回归中通过，工程门禁条目可标记验收；未知外部副作用、完整任务语义验收、Windows 原生行为与真实 Provider 评测等仍按原范围保留未完成，不以门禁通过代替整体目标完成。

### 长会话恢复列表容量（rc52，2026-09-06）

- 移除父会话派工超过 4096 条时拒绝查询的上限；目录遍历仅保留游标之后最小的 17 个候选 ID，返回 16 条及后续游标。
- 回归使用逆序写入的 4114 条真实持久化记录，覆盖首页、第二页、跨越 4096 后分页、尾页及空页，同时检查父会话隔离和不可覆盖的完成报告；测试通过（首次 92.47 秒，补取执行证据时重跑 98.22 秒）。
- workspace/all-targets Clippy -D warnings、格式及差异检查通过；233 个受检文件均不超过 3000 行。
- 每页仍为 O(n) 目录扫描；本次解决容量错误和候选集合内存增长，不宣称索引 I/O 已优化。整体目标仍按原验收项推进。

### 验证执行期间的快照变动（rc51，2026-09-06）

- 审查确认显式验收清单会逐项要求通过，不做不必要的范围扩展。另发现验证命令只绑定开始快照，执行中发生变动仍可能给原快照留下通过记录。
- 前台与受管后台验证命令成功结束后复核快照；变化或不可读则将该证据记录为失败并追加 verification-invalidated 说明，保留真实退出码，不把进程成功误写为验证成功。
- 新增快照变化/消失/读取失败以及真实 Ruby 命令修改快照后恢复原文件的回归；稳定重跑才重新获得通过证据。验证专项 14 项通过；扩大工具组后发现旧测试错误期待变动后的检查为 Passed，保留起始快照断言并改为失效、退出码 0 和明确说明。最终工具组 61 项全部通过（0.33 秒）；Clippy workspace/all-targets -D warnings、格式、差异检查通过。
- 此检查比较起止快照，不证明执行中短暂修改后恢复的 ABA 情形不存在；任意外部操作仍不能用 Git 快照证明。版本 rc51，233 个受检文件均未超过 3000 行。


### Web 重试状态渲染验收（rc50，2026-09-06）

- 新增真实 App + 本机 Runtime API 夹具及 Playwright 回归，未替换 React 组件、未连接真实 Provider。浏览器验证等待可见/停止可用、重载保留等待、真实轮询清除等待、停止只发一次作用域正确请求、终态重载及重试入口。
- rc50 浏览器回归通过，页面异常为空；检查两张截图，版本与等待/取消状态一致。结果为 target/web-runtime-retry/report.json、report.md 及 PNG；复现步骤见 WEB_RUNTIME_RETRY_QA.md。夹具进程已关闭。
- 首次测试选择器使用“停止”而非字典中的“停止生成”，未执行点击；修正后两次运行通过。Web lint、Node 语法及差异检查通过；源码规模检查扩展到 .mjs/.cjs，233 个文件均未超过 3000 行。
- 此证据覆盖后台 Agent 侧栏真实 Web 行为，不替代 daemon/Provider 集成，也不覆盖根聊天流停止入口；原完整请求等待条目仍需按剩余范围审计。


### 压缩后的未知效果约束（rc49，2026-09-06）

- 手工压缩先恢复缺失结果调用的 UNKNOWN 标记；自动和手工摘要保留含未知效果的完整工具批次，避免先保存摘要后丢失原参数，使 rc48 重试确认约束失效。
- 超大工具输出分页和末级批次归档均保留未知效果记录；仍超出窗口时沿用明确 ContextCapacity 错误，不静默删除约束。
- 新增手工压缩后再次恢复、末级归档后再次恢复及自动压缩的参数/协议配对回归；上下文专项 15 项通过（0.99 秒），扩大到 Agent/Worker 回归 88 项通过、1 个辅助入口由父用例调用（1.79 秒）；workspace/all-targets Clippy -D warnings、格式和差异检查通过。
- 版本 rc49，230 个受检文件均未超过 3000 行。该批修复约束跨压缩丢失，不代表未知外部副作用已完成对账。


### 未知效果调用重试约束（rc48，2026-09-06）

- 历史恢复在协议清洗前补入明确的未知效果结果，保留原调用 ID、名称和参数；该标记不表示工具成功，不伪造外部执行回执。重复恢复不会重复插入标记。
- run_command/create_file/edit_file/create_worktree/call_mcp_tool/spawn_agent 的同名同参数重试要求一次性确认，不受工作区自动写入或历史始终允许规则绕过；确认后仍走原工具权限及沙箱。JSON 对象字段顺序不影响匹配。
- 两项回归通过：重复恢复保留参数、字段顺序变化匹配；实际 Agent 重试在拒绝确认时不创建文件，允许一次时才创建。Core 全量 453 项通过、4 项默认忽略（2 个辅助入口由父用例执行，2 项旧/真实 livefire 未运行），Provider 契约 3 项通过；workspace/all-targets Clippy -D warnings、格式与差异检查通过。
- 边界：本批不能识别语义等价但文本不同的 Shell 命令，也不能确定外部副作用是否已发生；不能据此勾选完整执行恢复验收。无交互审批者默认拒绝不确定重放。
- 版本统一 rc48，230 个受检文件均不超过 3000 行。


### rc47 整体验证与范围审计（2026-09-06）

- 完整 workspace 测试退出码 0：CLI 387、脱离作业 13、Runtime 集成 22、Core 451、Provider 契约 3、Runtime Client 7、协议 31，共 914 项通过、6 项默认忽略；文档测试无失败。辅助子进程的内部通过数量未重复加入总数。
- 忽略项中 3 个辅助入口由父用例执行；公网 relay、旧 livefire 样本预检及真实 Provider livefire 未运行，不算通过。本次五场景 Ruby 预检另行执行，executed=0；Ruby 驱动 17 项/63 断言通过。
- Web lint/build、格式、差异检查通过；rc47 workspace/all-targets Clippy 在上一批已通过，本轮未改 Rust 代码。新增审计文档后 229 个受检文件均不超过 3000 行。Web 仍有既有大于 500 kB 的打包提示。
- 项目规则条目完成源码与测试范围对照：目录作用域和来源、读写前刷新、隐藏/忽略目录发现、超长全局规则失败、实际 Agent 延期写入及共享工种定义；将该条目标记已验收。其他整项仍保留未完成。
- 完整审计见 AGENT_RELIABILITY_AUDIT.md；本轮只更新验收文档，不增加版本号，不提交或发布。


### 手工压缩用量持久化（rc47，2026-09-06）

- Session 增加向后兼容的 manual_compression_usage，保存已报告调用数及输入/输出累计；它不表示缺失用量的调用已经完整计费。TUI 与非交互 /compress 在收到每个已知响应用量后先持久保存，再继续摘要处理。
- 用量纳入会话执行快照的并发合并规则，手工压缩完成或失败后刷新执行状态，防止旧内存历史覆盖账目；不修改普通运行检查点的用量，不把压缩账目重复加回后续轮次。
- 上下文专项 13 项通过（0.34 秒），覆盖账目重载、重复保存、并发标题保存及失败摘要。Session 回归 25 项通过（1.61 秒）；真实 CLI 压缩后退出再续跑通过（2.27 秒），已报告输入 5 / 输出 3 的账目在续跑后仍仅一次。集成测试首次因沙箱禁止本机端口绑定未执行，经权限允许后通过；workspace/all-targets Clippy -D warnings、格式与差异检查通过。
- 版本统一 rc47；228 个受检文件均未超过 3000 行。真实 Provider 评测仍待授权，路由/审核等其他辅助调用的完整账目不由本批宣称完成。


### 压缩调用用量与预算（rc46，2026-09-06）

- 自动压缩每个已知用量响应先更新输入/输出累计并保存检查点，再发出用量事件；空摘要、无效响应及带部分用量的中断响应也记账，备用模型不会抹掉此前消耗。
- 压缩消耗计入当前 Agent Token 预算；达到上限后返回 TokenBudgetExceeded，阻止下一备用模型或主模型请求。上下文准备失败时结算本轮内核事件租约，避免悬挂。
- 手工压缩发出已有 Usage 事件；此项尚不代表所有手工压缩客户端的持久用量及总任务辅助调用成本均已完整验收。缺失 Provider 用量仍不伪造为完整账单，真实评测 Token 限制暂保留。
- 新增自动压缩累计与检查点、预算耗尽阻止主请求、手工压缩空响应/中断备用调用用量回归，上下文专项 11 项通过；扩大到 Agent/子 Agent 回归后 82 项通过、1 个子进程辅助入口默认忽略（由父测试调用），耗时 1.18 秒。workspace/all-targets Clippy -D warnings、格式、差异检查通过；228 个受检文件均未超过 3000 行。


### 子模块快照与归因（rc45，2026-09-06）

- 子模块 HEAD 未变化时，内部暂存、未暂存、未跟踪内容仍纳入父仓库验证快照及归因；递归检查嵌套 Gitlink，覆盖隐藏子模块状态的 Git 配置。
- 未初始化目录使用明确标记，避免 Git 向上发现父仓库；嵌套深度最多 32 层，超限明确失败。链接按链接本身计算，不把替代 Gitlink 的符号链接当目录递归。
- 新增已脏子模块再次修改、暂存状态变化、未跟踪内容变化、稳定重复快照及未初始化子模块回归。Diff Review 专项 17 项通过（5.04 秒），覆盖新增回归及既有快照、归因、安全撤销检查。
- 本批首次构建因磁盘耗尽失败；清理本仓库可再生成的 Rust 增量缓存后，以 CARGO_INCREMENTAL=0 重试通过；workspace/all-targets Clippy -D warnings 通过（61 秒），清理后磁盘可用约 97 GiB。源码行数检查 227 文件通过，无超过 3000 行；格式与差异检查通过。
- 上一批 rc44 全 workspace 测试进程与 Clippy 已回收，退出码均为 0；不以输出截断中的推算数量作为统计。


### 前台 Worker 恢复与完成报告回收（rc44，2026-09-06）

- 前台派工也保存不可变任务记录，并建立父会话索引。父 Agent 的 `list_agent_recoveries` 按游标每页返回 16 条原子任务概要，`resume_agent` 只接受原 Worker ID，不允许覆盖任务、约束或已批准写集合；父会话不匹配时拒绝。检查点恢复提示明确指向这两个工具。
- 前台 Worker 在完整执行及 verifier 成功后保存完成报告，再返回父任务。恢复遇到该报告直接返回历史结果，不重复调用子模型、写工具或 verifier；即使原工作树已清理也能回收报告，并明确说明未重新验证当前文件。未完成的 Worker 仍恢复原历史与工作树，重新检查当前权限策略。
- Core 实际 Agent 工具回归通过：写入后取消前台调用，重建 Catalog，经列表与恢复工具续跑同一子任务；随后再次取报告，子模型调用次数保持不变，带写入计数的 verifier 只执行一次。增加严格参数、分页、父会话隔离、不可变报告和工作树清理后的报告回收检查。
- 真实 CLI + 本机模拟 Provider 回归通过（6.89 秒）：子任务写入和工具结果已落盘时终止父进程，恢复同一父会话，由模型调用列表及恢复工具继续原 Worker；再次取报告没有新增子模型请求，磁盘标记及已完成写调用均仅一次。原派工记录、历史与报告均保存在私有状态目录。
- 版本统一为 `0.72.0-rc44`；Windows 动态验收及外部真实模型评测仍未完成。本批证明恢复机制，不据此宣称真实模型评测完成率或整项目标已完成。

### Worker 派工重建与公开重试入口（rc43，2026-09-06）

- 后台 Worker 在启动前原子发布不可变派工记录，保存父会话、已解析工种、原任务包/约束/verifier、已批准写集合、模型和工作树位置；新建目录 0700、记录 0600，上限 4 MiB。不保存 Provider 凭据。原记录不允许被另一个任务覆盖。
- 显式重试在内存启动器缺失时重建 Catalog 派工，沿用同一 Worker ID 与检查点历史，重新检查当前工种、验证命令及沙箱，并保留原工作树。父会话、根工作区、Git 公共仓库、分支或工作树策略不匹配时拒绝恢复。
- Runtime 重试可从旧任务转交给同一父会话当前正在运行的新一轮；子 Agent 的新 started 事件更新 task_id，同时拒绝跨父会话接管。默认重试保留 Runtime 已记录的模型。首次构建 Harness 即分配父会话 ID，避免新会话派工记录缺失所有者。
- 实际 Catalog 回归验证：独立工作树写入完成后中断，丢弃 Catalog/后台注册表，再从记录重建并恢复同一 Worker；工具调用和写入仅一次，原 verifier/约束/写集合保留，旧目录复用。公开 API 回归验证：停止真实 Runtime、续跑同一父会话后，`agent.retry` 恢复原子任务 ID 和历史（本机模拟 Provider，25.28 秒）。子任务组 40 项通过、1 个辅助入口由父用例调用。
- 同时修复工作树根路径别名导致已批准路径与工具实际路径不一致的问题：创建工作树后规范化真实目录。此批恢复需要先续跑同一父会话，再显式重试子任务；旧版本没有派工记录的 Worker 无法凭空重建。Windows 动态验证和外部真实模型评测仍未完成。
- 此批启动器重建适用于后台派工。前台 Worker 的历史检查点已保留，但父进程中断后仍未提供对应的派工启动器重建入口；不能据此宣称所有子任务恢复场景都已完成。
- 最终 rc43 全 workspace：CLI 385、脱离作业 13、Runtime 集成 21、Core 443、Provider 契约 3、Runtime Client 7、协议 31，共 903 项通过、6 项默认忽略，文档测试无失败。忽略项仍为 3 个被父测试实际调用的进程入口及公网 relay/旧 livefire 预检/收费模型评测。Clippy workspace/all-targets `-D warnings`、格式、差异、Web lint/构建通过；225 个受检文件均不超过 3,000 行。

### Worker 跨 Catalog 写集合占用（rc42，2026-09-06）

- 原有文件认领仅在一个 Catalog 的内存集合里生效；新增共享 Worker 状态目录的按路径文件租约，Runner 在请求模型和执行 verifier 前取得整组租约，并持有至完整运行结束。独立 Catalog/进程不能同时取得相同目标。
- 对已存在路径及新文件的现存父目录解析真实路径，避免路径别名绕开占用。文件锁保留稳定 inode，进程退出由操作系统释放；取得后续路径失败时自动释放此前租约。没有配置持久状态目录的嵌入式调用仍沿用内存认领。
- 并发测试发现其他线程 fork 时可能短暂继承文件描述符，单靠关闭描述符会延长锁寿命；租约 Drop 改为显式解锁再关闭。增加重复描述符回归，验证原持有者退出后可以接手，旧描述符随后关闭不会释放新持有者的锁。
- 最终 rc42 全 workspace 回归通过：CLI 384、脱离作业 13、Runtime 集成 20、Core 442、Provider 契约 3、Runtime Client 7、协议 31，共 900 项通过、6 项默认忽略；文档测试无失败。忽略项中 3 个子进程入口由父用例实际调用，另 3 项是公网 relay、旧 livefire 样本预检及收费真实 Provider 评测，不能算已通过。子任务专项 39 项通过。Clippy workspace/all-targets `-D warnings`、格式、差异、Web lint/构建通过；222 个受检文件均不超过 3,000 行。
- 此项修复跨会话写冲突，不等于任务定义、启动器和重试入口已完成重启重建；后者仍待实现。Windows 动态行为仍未验收。

### 固定写入边界的中断恢复评测（rc41，2026-09-06）

- 评测新增 `interruption_recovery`：固定追加命令产生一行标记且对应工具结果已经持久化，检查点仍运行、待返回调用为空时注入中断；终止后重验边界，恢复同一会话。最终同时检查原调用仅出现一次、标记未重复、完成检查点、独立修复测试和文件保护。
- 恢复率只统计已核实注入中断的场景；未注入成功计为场景失败，不能伪装恢复成功。中断前在途用量无法完整取得，Token 保留 null。
- Ruby 判定与进程清理回归 17 项、63 个断言通过；五类样本预检通过，实际 Agent 执行数为零。真实 CLI + 本地模拟 Provider 的中断恢复专项通过（2.56 秒），验证持久工具结果、进程退出后会话锁释放、同会话续跑和一次写入；不代表真实模型修复样本通过。
- 本批完整 `headless_runtime` 集成组 20 项通过（38.47 秒）；workspace/all-targets Clippy 无警告，格式与差异检查通过。中断请求可能尚未发出，测试允许该次请求缺席，仍严格验证原工具调用和实际写入只有一次。最终全 workspace 总验收仍待完成，最近全量记录是 rc36。
- 221 个受检文件均不超过 3,000 行；版本统一为 `0.72.0-rc41`。外部 Provider 评测仍待授权，整体七类问题仍按上方清单验收，不宣称全部完成。

### 第一批实现（2026-09-06）

- 分支：`codex/agent-reliability`；版本统一为 `0.72.0-rc1`，未提交、未推送。
- 增加 `checkpoint` 模块：Core Session 保存执行历史、轮次、用量及未得到结果的调用 ID；Runtime、headless 和本地 TUI 接入；取消后先等待任务结束，再从磁盘读取检查点。Provider 错误保持原错误类型，恢复提示明确禁止盲目重放副作用。
- `agent/context` 保留所有原始 System/User 消息，摘要序列化工具调用参数；工具输出归档可通过 `read_tool_output` 按 Unicode 字符分页取回；压缩边界保持完整工具往返，预算计算包含附件、工具参数及工具 Schema。保护内容无法放入窗口时明确报错，不偷偷删除约束。
- `project_rules` 按操作目录加载规则；新发现/发生变化的规则会让当前动作延期，下一轮模型读到完整规则后才能重发。Shell/MCP 的路径不透明，调用前发现工作区内规则文件；公开工种名称由共享定义生成。
- 输出截断不会执行其中的工具调用；最多续接三次，然后标记部分结果。Goal 不再将重复结果、失败工具或仅有后台任务存活计作新进展。主任务验收契约尚未实现。
- 拆分 Agent/Tools/SessionStore 测试、TUI 状态与事件循环、Daemon TaskManager、后台 Shell；增加 `scripts/check_source_size.rb`，同时生成 JSON/Markdown 并接入 CI。检查 192 个手写源码/文档，均不超过 3,000 行。
- 验证：检查点专项 4 项通过；上下文首批 7 项通过；核心库在具备本地端口/进程查询权限的环境中 340 项通过、2 项忽略；`cargo check --workspace --offline --quiet` 通过。随后增加了预算保留、共享工种和补充用例，需在最终门禁重新验证。

### 共享命令入口首批（2026-09-06）

- 新增 `execution` 模块，前台 `run_command` 接入：显式沙箱不可用时拒绝启动；stdout/stderr 并发排空，分别只保留配置上限的尾部，避免先无限缓冲再截断。
- Unix 命令创建独立进程组；取消和超时会清理该组。观察进程退出使用 `waitid(WNOWAIT)`，在回收组长之前清理后代，避免组长 PID 回收后误杀复用 PID；组长退出而后代仍占管道时也能够结束。
- 已验证共享入口 5 项测试（大量双路输出、流排空、超时、孤立管道、取消后无延迟写入）；Tools 原有 43 项回归通过；`cargo check --workspace --offline --quiet` 通过。
- 尚未把后台、脱离作业、Worker/verifier 接入共享入口；Windows 后代清理、输出截断标记及归档仍需补齐。上述局部验证不能证明统一执行验收项完成。

### 后台与 Worker 执行接入（2026-09-06）

- 后台 supervisor 的请求显式携带沙箱，执行与单测均复用 `execute_background` / `execution::run_capture`；父进程断开通过丢弃执行 Future 清理进程组。结果信封读取上限考虑 JSON 转义膨胀，避免输出接近上限时截坏 JSON。
- Harness 将同一沙箱传给 SubagentCatalog；独立 checkout 映射工作区内可写根，保持细分目录范围与显式外部根。Worker 工具及运行时 verifier 都继承该策略，verifier 改为有界输出和共享清理。
- 验证：真实 CLI supervisor 2 项（正常完成/父断连清理、只读写入拒绝）通过；权限映射 2 项通过；Worker runner 7 项（含 verifier 只读拒绝）通过。Windows 后代清理和脱离作业仍未完成，不勾选统一执行整项。

### 脱离作业接入（2026-09-06）

- 脱离作业改为独立 CLI supervisor，通过有长度边界的 stdin JSON 传递命令、工作区、沙箱和超时；先写作业元数据，再发送命令。父 Runtime 正常退出不会取消作业。
- 复用共享执行与有界输出；结果日志和详细状态写完后才原子发布退出标记。超时使用退出码 124、终止使用 137；Unix supervisor 收到 TERM/INT 时取消执行并清理后代。新建记录目录 0700、日志 0600。旧日志读取也改为 seek 后限量读取。
- 原来依赖单测进程的 7 项作业测试迁移到真实 CLI 集成测试，并增加真实父进程退出后只执行一次、只读沙箱、超时阻止延迟副作用、输出磁盘上限、终止清理和权限检查。
- 最终本批集成验证在允许进程查询的环境中 12 项通过，1 个子进程辅助入口按设计忽略（由主用例实际调用），包含 TERM 清理与权限断言；沙箱内 PID 复用测试因禁止 ps 失败，放开读取后通过。workspace 编译检查通过；194 个受检文件无超过 3000 行的项。
- 仍需补充执行中日志可见性与异常时部分输出保留；当前脱离日志在结束时发布。Windows 后代清理和其余完整验收项仍待实现。

### 运行中日志与异常输出（2026-09-06）

- 共享捕获状态保留 stdout/stderr 有界尾部；超时及执行 IO 错误携带已捕获输出。前台超时工具错误和验证报告保留日志，verifier 的错误传播也获得相同上下文；成功结果明确标记省略的旧字节数。
- 脱离作业运行中持续原子发布 0600 日志快照，每个日志文件保持输出上限，TERM/超时结束不会覆盖已有流日志；终止状态单独保存，避免重复回灌整份日志。
- 共享执行 6 项、Tools 43 项及 workspace 编译检查通过；完整作业集成组 13 项通过，1 个辅助入口由主用例实际调用。真实运行中日志专项先确认 `Running` 且 stdout/stderr 可读，再验证超时后仍保留；启动观察窗口独立于命令执行时限，避免把 supervisor 启动延迟误判为日志不可见。
- 仍需验收 Windows 后代清理、取消后的根会话日志/用量恢复、完整上下文归档、其余六类问题的未完成部分。大输出持续日志的磁盘写入频率后续评估，当前保证有界和可读。

### Provider 限流与等待首批（2026-09-06）

- 三协议共同使用的 HTTP 请求层支持 429/5xx 的 Retry-After（秒数、HTTP 日期）；整数溢出不会退化成快速重试。没有有效头时采用有限退避，其他 4xx 保持直接报错。
- 尝试与等待共享请求期限；服务端等待要求超过剩余期限时返回 RetryDeferred 并保留原始 HTTP 错误，禁止通过缩短等待提前重试。取消 Future 后不会再发请求。
- 响应改为分块限量读取：错误正文最多 8 KiB 后截断，成功正文超过 16 MiB 明确拒绝，避免先无限读取再截断。
- 本批 11 项请求层测试通过，覆盖真实本机 HTTP 服务的等待时序、取消后请求次数、超长等待拒绝、超大且未结束错误响应的提前停止读取，以及既有重试；workspace 编译检查通过。
- 规范依据：https://www.rfc-editor.org/rfc/rfc9110.html#name-retry-after 。三协议流式解析、重试等待事件及 UI 展示、独立只读工具有限并发仍未实现，不能以这批重试验证替代整项完成。

### SSE 公共解码与传输读取（2026-09-06）

- 增加增量 SSE 解码器：跨块 UTF-8、CR/LF/CRLF、起始 BOM、多行 data、空 data 和事件名重置；忽略注释/重连字段，不自动重放生成。无效 UTF-8 明确报错，防止默默改写工具参数。
- 单事件默认 1 MiB、整流 32 MiB 上限覆盖未结束行和多行累积；EOF 不派发半个事件。公共网络读取器按事件回调，在协议显式完成后立即返回，无完成事件的 EOF 报错，整个读取受期限约束。
- 7 项测试通过，包括 5 项解码测试和 2 项真实网络测试：服务端等待首事件被消费后才发送完成标记并保持连接不关闭，证明客户端无需等 EOF；有效增量后没有完成标记的 EOF 仍被拒绝。195 个受检文件未超过 3000 行。
- 格式依据：https://html.spec.whatwg.org/multipage/server-sent-events.html#event-stream-interpretation 。目前是公共基础层；三个 Provider 仍需接入请求、协议增量组装、用量和中断保存、界面事件。不得将解码测试当成完整三协议流式能力已完成。

### Chat Completions 流式入口（2026-09-06）

- 增加 Provider 的 complete_with_events 与 TextDelta/Usage/RetryWait 事件。普通 Provider 保持兼容默认实现，Chat 实现流式请求并启用 include_usage；Agent 主循环和界面尚未切换到该入口。
- HTTP 公共重试层现在只在成功响应头交付前重试；正文/流中断不重放已开始的生成。流式读取与重试共用同一期限，重试等待能发出事件。
- Chat 工具参数按索引组装，校验调用身份、JSON 对象参数、数量和结束原因；length/content_filter 清除工具调用。断流返回 StreamInterrupted，携带部分文本和已知用量，不包含可执行工具调用；没有收到用量时保留 None。
- Provider 层 36 项测试通过，含真实请求的 stream/include_usage、中文增量与用量、断流仅请求一次、半截工具参数不执行，以及既有重试/SSE/协议编码回归。
- 协议依据：https://github.com/openai/openai-python/blob/main/src/openai/types/chat/chat_completion_chunk.py 。Responses、Anthropic、主循环检查点和 UI 事件接入仍待完成；不能将新增接口视为用户已获得完整流式体验。

### Responses 流式入口（2026-09-06）

- Responses 实现 complete_with_events，发送 stream=true/store=false；文本按 output/content 索引组装，工具参数按 item_id 累积，校验事件类型、可选序号单调性和 response 身份。
- 终态事件必须与 response.status 对应。completed 核对已观察文本、参数及工具身份，拒绝丢失调用、重复调用 ID、不完整对象参数和未结束输出项；incomplete 保留部分文本与用量且不返回工具。EOF/协议错误携带部分结果，不重发请求。
- 抽出协议共享测试 HTTP 服务和事件记录器，供 Chat/Responses 及后续 Anthropic 共用。首批 Provider 全组 43 项通过，workspace 编译检查通过；最终 Responses 专项 7 项通过，包含工具身份消失/改变校验。200 个受检文件均未超过 3000 行。
- 协议依据：https://developers.openai.com/api/reference/resources/responses/streaming-events 。Anthropic、Agent 主循环的检查点/事件接入与用户界面仍未完成，当前不能宣布三协议流式验收完成。

### Anthropic 流式入口（2026-09-06）

- Anthropic 实现 complete_with_events，按 message_start、内容块开始/增量/结束、message_delta、message_stop 校验生命周期；索引有界，拒绝已关闭块的增量、重复块、结束原因与工具不匹配等情况。
- tool_use 输入按 JSON 增量组装，最终校验对象与唯一调用 ID；max_tokens/pause_turn/model_context_window_exceeded 等部分结果不返回可执行工具。断流错误保留正文和已知用量，不重发请求。
- message_delta 用量是累计值，覆盖已有值而不重复相加；保留开始阶段输入量。思考和签名内容不进入 TextDelta 或最终正文。
- Provider 全组 50 项通过，新增 6 项 Anthropic 测试覆盖累计用量、工具截断、生命周期、思考内容隔离、真实流式请求及断流单次请求。
- 协议依据：https://platform.claude.com/docs/en/build-with-claude/streaming 。三个 Provider 均已有流式入口，Agent 主循环仍调用 complete，下一步必须接入事件、检查点和 UI；当前仍不算用户端流式体验完成。

### 流式主循环与部分完成状态传播（2026-09-06）

- Agent 主循环已切换到 complete_with_events；文本和累计用量快照写入检查点，断流只保留文本与已知用量、不执行半截调用。忽略 stream 参数并返回 JSON 的服务端复用同一次响应解析。
- Harness 保留 AgentStopReason，Runtime Task/Turn/Agent 及公开协议新增 Partial；事件使用 task.partial / turn.partial，输出信封携带 stop_reason。部分轮次保存消息结束边界，Session 可继续接收后续任务。
- headless 保留部分文本并输出 partial JSON，再使用非成功退出码；TUI、Web 和通知状态增加部分完成，Web 文案覆盖中英日，避免发送成功通知。
- 部分状态专项 3 项通过，包含三种部分停止原因 JSON 和会话重新打开后的 Partial/消息边界。Web 类型检查及构建通过；203 个受检文件均不超过 3000 行。
- 尚需真实 Runtime/headless 端到端验证、客户端事件过滤及继续/重试入口审查；模型自报完成仍未绑定验证证据。流式逐增量全量保存的性能和并发更新问题仍未解决，不能勾选整项。

### Runtime/Web 部分完成端到端回归（2026-09-06）

- 真实 headless 请求连续三次 length 截断后返回 partial/incomplete、退出码 5，并保存 Partial 轮次与消息边界；同会话下一轮正常完成，总请求数为 4，未重放旧生成。
- 修复 Web SSE 转发漏识别 turn.partial 导致永久等待的问题；浏览器接收 partial 后收尾，已停止会话的重连也返回 partial，保留正文且不额外请求 Provider。
- 新增真实 Web SSE 测试，验证流会关闭、重连不是 error/completed、请求次数不增加。完整 headless_runtime 集成组 14 项通过，Web 类型检查和构建通过。
- TUI 和 Harness 共用 task_stopped 通知入口，部分结果使用 attention_required/partial；补充 Partial Agent 的 completed_at 和清理 current_tool，避免 UI 继续累计运行时间。
- 后续仍须审查子任务的 Partial 状态生产、重试/工作树 review 入口及渲染行为；本批不代表主任务证据验收、持久化一致性或完整七项范围已完成。

### 会话原子字段更新首批（2026-09-06）

- SessionStore 增加 update，在稳定锁文件的跨进程排他锁内完成加载、字段修改和原子替换；save、set_pinned、delete 使用同一锁。锁文件不删除，避免旧描述符和新 inode 同时被不同写者锁住。
- 检查点改用 update，仅替换执行字段；Runtime 重命名和模型切换改为字段更新，置顶读改写也在锁内，避免这些入口互相覆盖消息或偏好。
- 7 项检查点测试、14 项 Session 测试通过；新增多 SessionStore 实例并发更新，验证 75 次执行计数无丢失，同时标题和置顶保留。workspace 编译检查通过。
- 尚未解决持有旧快照的 save 在之后覆盖新值的问题，也尚未验证跨进程竞争和 Swift 外部写者；继续迁移或为旧快照保存引入冲突检测。逐文本增量整份保存的性能问题仍在，不能宣称持久化整项完成。

### 旧会话快照的元数据合并（2026-09-06）

- Session 在 load/list/save/update 后保留仅内存的元数据基线，保存时在锁内对标题及来源、置顶、模型、profile、config、goal 做三方比较。调用方未修改的字段采用磁盘最新值；双方不同修改同一字段则报 ConcurrentUpdate，错误只包含字段名。
- 自动派生/摘要标题与用户标题竞争时优先保留用户标题，并继续保存执行结果。基线不写入 Session JSON，不改变持久化 schema。
- Core 全量 382 项通过、2 项原有忽略；新增旧执行快照保存、显式冲突不写盘、自动标题竞争三项回归。真实 headless_runtime 全组 14 项通过，覆盖会话续跑、标题、部分结果和 Web 重连。204 个受检文件均不超过 3000 行。
- 此批只解决上述元数据的旧快照问题；消息执行字段的并发所有权、无基线的外部反序列化保存、删除后旧写者恢复文件、Swift 外部写者与高频流式磁盘 IO 仍须审查，不能将整项勾选完成。

### 独立只读工具有限并发（2026-09-06）

- 全部由 read_file/list_directory/search_files/grep_files/read_tool_output 组成的批次可并发，最多 4 个在途调用；混合写操作、单调用、任意 lifecycle hook 配置保持原串行路径，外部/MCP/命令工具不推断为只读。
- 并发前预检整批目录规则；规则更新使整批延期到模型读取新规则之后。工具仍走现有执行与权限入口，不绕过路径策略。结果以模型原始顺序写入历史和检查点；没有派生脱离当前 Future 的后台任务。
- 使用实际 read_file 与事件屏障验证 8 个读取确实重叠、最大并发为 4、返回顺序稳定，并验证混合写批次不并发。Core 全组 383 项通过、2 项原有忽略；205 个受检文件均不超过 3000 行。
- 后续继续评估真实长任务吞吐、UI 多在途工具展示，以及取消时部分只读结果的及时落盘；本批不代表所有七项问题已验收。

### 子任务截断与 verifier 成功的误判修复（2026-09-06）

- 子 Agent 的 MaxTurns/Incomplete/BudgetLimited 不再以 Ok 报告进入后续 verifier 成功路径；保留部分报告及停止原因，通过现有失败通道交回父任务。
- 新增真实 Catalog 调用回归：Provider 连续返回 length 截断，配置始终成功的 true verifier，仍必须返回部分报告错误，不能产出 passed 结论。子任务整组 30 项通过，workspace 编译检查通过。
- 当前部分子任务沿用 Failed 生命周期，尚需单独贯通 Partial 与停止原因，避免与真实工具失败混淆；主任务的完成验收契约和证据绑定仍未实现。此修复仅消除已确认的错误成功路径，不作为主任务完成判定已解决的证据。

### 子任务 Partial 生命周期贯通（2026-09-06）

- 增加 SubagentPartial 结构化错误，保留停止原因、轮次和部分报告；前台 Catalog 和后台 TaskResult 均映射为 Partial，后台不再伪造成功退出码，也不归为普通执行失败。
- Partial 贯通子任务生命周期事件、Runtime Agent、注意事项状态、Web 工具状态及独立遥测类别；部分子任务允许重试和工作树审查，继续保留待处理可见性。
- 子任务整组 31 项通过，其中真实 Catalog 截断用例断言 Partial 完成事件，另覆盖前后台状态转换；Web 类型检查及构建通过，workspace 编译检查通过。205 个受检文件均不超过 3000 行。
- 尚需真实后台子任务端到端与重试集成验证；主任务证据验收、子任务持久检查点、流式 IO 优化与其余清单仍未完成。

### 验证证据绑定执行前快照（2026-09-06）

- 修复 Harness 在验证命令结束后采集快照的问题：ToolRegistry 在审批完成、命令启动之前采集 snapshot_id，并随 CommandVerification 返回；普通后台命令每次重试也重新采集。
- Harness 不再用执行后的工作区补造证据快照；没有捕获到快照时不登记验证记录，已捕获的 ID 交由 Runtime 现有 exact_snapshot 校验。这样执行期间文件变化会使结果过期，而不会认证变化后的文件。
- 验证辅助逻辑拆到 tools/verification.rs，Tools 主文件降至 2888 行；206 个受检文件均不超过 3000 行，workspace 编译检查通过。
- 两项命令验证专项通过，包含真实命令修改文件后仍保留执行前快照 ID，以及验证报告的长度/敏感命令过滤；Runtime 的过期快照拒绝专项通过。
- 仍需把合格证据接入主任务完成门禁、完善验收条件清单；脱离作业尚未接入此证据捕获，异步记录失败的可观测性仍需处理。不得将本批证据时序修复视为主任务验收已完成。

### 主任务当前快照验证门禁首批（2026-09-06）

- ToolRegistry 保留有界验证记录，主循环记录本轮初始快照。工作区快照变化后，候选终稿必须有当前快照的通过记录；按命令取最新结果，任何尚未解决的失败检查都阻止放行。
- 缺证据会注入继续验证指令，连续三次无证据收口返回 Unverified，沿现有部分结果链路呈现；Goal 完成标记同样经过门禁。预算收尾仍按预算停止语义交接，不强行扩张已耗尽预算。
- Core 全量 388 项通过、2 项原有忽略，覆盖旧证据失效、失败检查不能被另一个通过检查掩盖、重新通过后可放行，以及缺验证时不结束为成功。
- 补充 Goal 完成标记绕过专项通过；真实 headless_runtime 全组 14 项通过，确认普通问答、会话续跑及 Web 部分结果流程仍正常。
- 仍有明确未完成范围：未配置/无法取得初始快照的环境尚无此门禁；只验证本轮快照变化，不能替代完整验收条件清单、外部系统副作用与非测试类证据；恢复轮次必须继承未验证变更状态，当前仅比较本轮初始快照。命令分类器对复合/自定义验证命令仍有限制。这些缺口未关闭前不勾选完成判定整项。

### 恢复执行继承验证基线（2026-09-06）

- 检查点元数据新增可选 verification_baseline，兼容旧记录；首次执行在模型请求前初始化，后续每次检查点保存均携带同一基线。
- SessionCheckpointSink 对未完成记录返回原始基线，重建 Agent/Store 后不会把未验证文件当作新的干净起点；已完成记录不强制继承旧基线。读取恢复状态失败会在开始执行前返回检查点错误。
- 新增重建全部运行对象的两轮测试，两次均返回 Unverified，磁盘记录始终保留初始基线；检查点组 9 项通过（含恢复读取失败阻止执行），真实 headless_runtime 全组 14 项通过，workspace 编译检查通过。
- 旧版无基线记录、没有快照支持的工作区、持久验证证据重新载入和并发会话执行所有权仍待完善；整体完成门禁与七项范围尚未全部验收。

### Web 流式正文与等待事件接入（2026-09-06）

- 修复 Web client_event 过滤时丢失 assistant_text_delta 正文的问题；只传递正文和事件类型，重试等待生成中英日标签，用量快照不重复展示。
- Web 页面把增量累积到当前运行预览，每轮开始重置，最终或部分结果到达时清除预览，避免与最终回答重复；重试等待进入现有状态条。
- 真实 Web/Runtime/Provider 集成测试通过：Provider 在客户端确认收到首段文本之前不发送完成事件，随后释放并验证最终正文及关闭。此测试不依赖耗时阈值推断流式行为。Web 类型检查及构建通过。
- 页面实际浏览器渲染、断流后的预览恢复、高频增量渲染性能和子 Agent 流式事件仍需完整验收；这批传输证据不替代 UI 动态验收。

### 规则发现与全局规则错误传播（2026-09-06，0.72.0-rc2）

- 回归测试先复现 `run_command` 漏掉 `.gitignore` 目录中的规则；扫描关闭搜索忽略过滤，覆盖 `.ignore`、隐藏目录及 Shell/MCP/派工入口，仍排除 `.git` 且不跟随目录符号链接。目录规则整组 6 项通过。
- 根任务与 Worker 共用全局用户规则加载器；缺失/空文件允许省略，读取失败、非法 UTF-8 和超过 1 MiB 显式失败。加载器和提示词 5 项通过，包含上限边界与不静默截断。
- 前后端版本同步为 0.72.0-rc2，延续整个可靠性目标。修正 Clippy 指出的并发返回类型、Provider 借用/布尔表达式、测试模块位置和测试读取检查。
- 本批验证：Core 全组 393 项通过、2 项原有忽略，Provider 契约集成 3 项通过；workspace all-targets Clippy（-D warnings）、格式、diff 检查与 Web 类型检查/构建通过。206 个受检文件无超过 3000 行的项。Web 构建仍提示大于 500 kB 的 chunk。
- 本批不能证明全局规则的外部进程配置覆盖、Worker 恢复、真实页面、Windows 清理或完整长任务评估完成；其余验收项继续保留未勾选。

### 尚需继续（不以当前实现替代完整范围）

rc12 静态检查：workspace all-targets Clippy（-D warnings）、格式、diff、行数检查通过；210 个受检文件均未超过 3000 行。最后一次完整 workspace 通过仍为 rc9，本轮完成 Core 全量回归。

0.72.0-rc12 流式内存路径：CheckpointRecorder 复用本轮历史和工具配对结果，仅向当前助手消息追加文本并更新累计用量；没有持久 sink 时也不再复制历史。每个增量仍调用 sink 并传播失败。内核事件加入后同步本轮检查点，失败释放事件租约。历史缓冲区复用和中文分片拼接回归通过；取消测试改为等待第二轮已持久化边界，避免固定 200 ms 在同步落盘后提前取消。Core 完整 408 项、Provider 契约 3 项通过，3 项标记忽略。此批只减少内存/CPU 重复工作，SessionSink 仍全量序列化与写盘；增量日志及真实吞吐评估仍未完成。

rc11 静态检查：workspace all-targets Clippy（-D warnings）、格式、diff 与行数检查通过；210 个受检文件均未超过 3000 行。本轮未重跑完整 workspace，最后一次全量通过仍为 rc9。

0.72.0-rc11 保存边界：SessionStore 使用 create_new 的 0600 临时文件，write_all 后 sync_all，再 rename；Unix 还同步父目录，错误继续向检查点调用者传播。会话专项 25 项通过，含创建/替换后的私有权限；检查点相关 16 项通过、1 个辅助入口由跨进程测试调用，新增 Worker 请求中取消后保留已提交历史并重新获取所有权的回归。尚未做掉电故障注入；流式全量保存的写放大仍待通过增量日志解决，不能以同步落盘实现宣称性能项完成。

rc10 验证：子任务整组 32 项通过；真实 Runtime 公开派工测试通过并新增读取 workers Session 的 Completed 检查点及助手历史断言。workspace all-targets Clippy（-D warnings）、格式、diff、行数检查通过；210 个受检文件均未超过 3000 行。本批未重跑完整 workspace，最后一次全量通过为 rc9。

0.72.0-rc10 Worker 检查点首批：SubagentCatalog 接受显式 state_home，Harness 配置独立 workers 目录；子任务工具归档使用该目录，run_once 通过持久 Session 和 claimed checkpoint 执行。同一 Agent ID 的重试加载既有历史与不确定操作提示，工作区不匹配拒绝恢复；Unix 状态根目录 0700。重建 Agent/Store 后继续的回归通过，已完成写工具往返保留且文件不重写；子任务整组 32 项通过。尚未持久化完整派工清单/重建重试 launcher，Runtime 进程重启后的自动恢复仍未完成，不能将历史落盘等同于完整 Worker 恢复。

rc9 完整验证：cargo test --workspace --offline 退出 0；CLI 374、脱离作业 13、headless_runtime 15、Core 404、Provider 契约 3、Runtime client 7、Runtime protocol 31 项通过，共 847 项，5 项标记忽略（含由主用例实际调用的子进程入口）。workspace all-targets Clippy（-D warnings）、格式、diff、行数检查与 Web 类型检查/构建通过；Web 仍有既存大 chunk 警告。版本统一为 0.72.0-rc9。测试门禁通过不覆盖清单中尚未实现的 Worker 持久恢复、流式 IO 优化、Windows 后代清理、真实长任务评估和动态页面验收。

0.72.0-rc9 完整回归首轮发现 Runtime 分叉两项失败：直接修改来源 Session 的 ID 保留了旧保存基线，被删除保护正确拒绝。新增显式 fork_snapshot 生成新身份并清除来源基线；Runtime 分叉走该入口，新增原会话字节不变的回归。修复后完整 workspace 测试继续运行，不把此前专项通过视为全量通过。

rc8 验证：结束快照抗后续历史变化回归通过，真实 headless_runtime 15 项通过；workspace all-targets Clippy（-D warnings）、格式、diff 及源码行数检查通过，209 个受检文件均未超过 3000 行。版本统一为 0.72.0-rc8，未提交/推送。

0.72.0-rc8 Runtime 结束快照：RuntimeHarnessOutcome 携带最终 session 的 message_end/message_generation。TaskManager 在发成功输出/登记终态之前持久化该快照，失败则走失败路径；complete_task 保留已有结束快照，不被后续 Core 写者改变。未携带快照的兼容调用仍沿用读取 Core 的旧路径。新增回归在记录边界后追加新消息并推进压缩代数，验证完成登记和重新打开 Runtime 后仍保留原结束边界。该快照方案不等于所有写者都参与执行锁，Worker 持久化和外部更新等原范围仍未完成。

rc7 验证：起点校准/恢复消息替换回归通过；真实 headless_runtime 15 项通过；workspace all-targets Clippy（-D warnings）、格式、diff 及源码行数检查通过。209 个受检文件无超过 3000 行项。

0.72.0-rc7 Runtime 起点：execute_runtime 在加载运行历史/构建 Harness 前 claim 会话，并把所有权交给 execute_noninteractive 保持到最终保存。锁内 prepare_execution 校准非恢复轮次的 message_start/message_generation；恢复轮次严格校验原起点、代数、用户正文及附件，拒绝错位重放。新增队列历史变化/替换恢复消息回归通过。成功 Harness 返回后到 Runtime complete_task 读取 message_end 之间仍存在结束边界窗口，需让所有权或精确结束快照覆盖该步骤；此批不宣称首尾边界整体验收。

0.72.0-rc6 手工压缩：本地 TUI 在读取压缩历史之前 claim 会话，结果事件携带所有权，接收端保存后释放。竞争者存在时在 begin_turn/派发任务之前拒绝。新增实际 dispatch 与消息通道回归，验证拒绝不创建 local_turn、结果抵达后仍持锁、保存后可重新获取。Runtime 队列进一步核查：claim_next_turn 仅写 Turn 元数据与消息边界，正文在 Harness 保存；消息边界预先记录与实际执行锁获取之间的窗口仍需处理。

rc6 验证：新增手工压缩 dispatch 回归通过；workspace all-targets Clippy（-D warnings）、格式、diff 检查通过，209 个受检文件无超过 3000 行项。此批未重跑完整 workspace 和真实 Runtime 集成，不将局部证据当作整体验收。

0.72.0-rc5 提交入口保护：增加 ClaimedSessionCheckpointSink，Harness 与本地 TUI 提示词/通知入口在用户消息保存前 claim。Harness 持有至最终保存与后台续轮结束；同一个 claimed sink 用原子状态拒绝并发 run，可串行复用。执行 guard 显式 unlock，避免并行子进程在 exec 前短暂继承描述符延迟释放。检查点专项 13 项通过，辅助入口由跨进程测试实际执行。真实 headless_runtime 15 项通过。随后让 TUI 完成事件携带所有权直到接收端保存完毕，界面回归继续验证。Runtime 提交队列的前置保存、TUI 手工压缩、普通外部更新与 Worker 持久化仍需继续审查，不能据此宣布所有权完整完成。

CHANGELOG 在 0.21.0-rc62 版本边界拆分到 docs/changelog-history.md，拆分时校验原始内容重组完全一致，双方保留入口链接；209 个受检文件均未超过 3000 行。

rc5 TUI 完成事件变更后的现有界面回归 127 项通过；workspace all-targets Clippy（-D warnings）、格式、diff 和行数检查通过。此组覆盖已有状态/会话交互，不能替代多客户端竞争的动态端到端验收。

0.72.0-rc4 执行所有权首批：CheckpointSink 提供整个 run 生命周期的 guard；SessionCheckpointSink 通过独立稳定 inode 的文件锁实现跨进程互斥，在 Provider/工具调用之前获取，结束、错误或取消时释放。不使用 PID 过期判断，也不删除锁文件。元数据锁与执行锁分开，其他会话及置顶可继续。此范围覆盖 run_checkpointed 内的检查点更新，尚不覆盖调用者在进入 run 之前的用户消息保存、普通 SessionStore.update 外部写者、Worker 的非持久运行；这些入口仍需统一所有权协议。

本轮补充（0.72.0-rc3）：会话基线新增流式计算的 SHA-256 执行快照指纹，消息、检查点、压缩边界和运行游标整组保留/冲突检测；旧元数据保存保留最新执行快照，竞争执行保存和无基线对象覆盖现存会话被拒绝。已加载会话被删除后的保存返回 NotFound，防止该旧写者恢复文件。refresh_execution 同步执行基线，保留元数据基线以继续检测重命名冲突。真实 headless_runtime 15 项通过（在最后的 refresh_execution 补充之前编译）；专项回归与最终静态检查继续补记。该保护仍不替代执行所有权：两个 CheckpointSink 的字段更新、未参与锁协议的外部写者，以及无基线对象重建已不存在的 ID 仍须独立处理。

rc3 检查：会话专项 23 项、检查点专项 9 项通过；workspace all-targets Clippy（-D warnings）、格式与 diff 检查通过。208 个受检文件均不超过 3000 行。

rc4 检查：检查点专项 12 项通过，1 个辅助入口由跨进程用例实际执行；覆盖竞争运行拒绝、不同会话并行、置顶不阻塞、真实子进程锁竞争、取消/失败释放。workspace all-targets Clippy（-D warnings）、格式与 diff 检查通过，208 个受检文件均不超过 3000 行。真实 Runtime 全链路与调用前消息保存所有权仍待验证和完善。

1. 完成主任务验收契约、证据与当前变更绑定；继续审查子任务部分状态，以及检查点的中断恢复与 Turn 边界兼容性。
2. 将沙箱、超时、有界输出和进程组清理统一用于前台、后台、脱离作业、子 Agent、verifier，显式沙箱不可用不得回退裸 Shell。
3. 三协议流式输出、可取消的 Retry-After 和独立只读工具有限并发已实现首批；继续验证高频落盘/渲染性能、子 Agent 事件与真实页面阶段展示，不以传输测试代替动态验收。
4. 扩充实际长任务靶场与 JSON/Markdown 指标，验证中断、压缩、重复失败、子任务集成及脏文件保护。
5. 验证归档输出隔离/权限、Worker 的持久输出目录与规则继承；全量 workspace/前端测试、Clippy、格式、行数检查与用户可见行为验收。
6. 完成 CHANGELOG 和 PRODUCT_OVERVIEW 的最终说明。目前开发中标记有意保留。

超过 2,000 行但未超过 3,000 行的文件暂保留既有职责：Diff/API/Web/Main 模块已有领域边界；Tools/Daemon 正随本次执行层改造进一步拆分。CHANGELOG 接近上限，最终记录增加时将按历史版本分档，保留全部记录。

### 验证命令退出状态可信性（0.72.0-rc13，2026-09-06）

- 验证证据识别改为保守解析单个前台命令；拒绝 Shell 复合、管道、后台、重定向、展开与不完整引号，保留引号内字面参数，不再大小写折叠可执行文件名。
- 帮助、列表、仅编译命令不计入测试通过；cargo nextest 只识别 run 子命令。复杂命令仍可按原工具权限执行，但当前不能直接成为完成证据。
- 真实工具回归运行退出 7 的 Ruby 脚本并以 || true 包装：确认脚本确实执行、工具返回成功、验证记录为空、完成判定拒绝。工具组 47 项通过；workspace 全目标 Clippy -D warnings 通过。
- 版本同步至 rc13；源码行数检查 210 个文件无超限。完整 workspace 测试和整体验收尚未完成；缺失基线、验收契约、恢复操作核对、Windows 进程树及真实任务评估仍需继续。

### 首次响应计时（0.72.0-rc14，2026-09-06）

- StreamEvents 在首个非空文本增量到达、持久化和 UI 转发之前记录时间；AgentOutcome 首次响应延迟使用该时间，后续轮次不会覆盖。无流式文本时保持完整响应计时，口径明确为从运行开始（含上下文准备）。
- 更新 Core/TUI 注释，区分首答、后续生成及总耗时；Runtime 客户端仅观察用量时仍使用兜底口径，尚未将这一局部改动视为全端指标一致。
- 新增状态测试覆盖空文本、用量、重试事件不触发计时，以及后续文本不覆盖；真实 Agent 测试在首字后延迟结束响应，验证首答没有等到响应完成。
- 完整 Core 在允许本机 HTTP 端口的环境中 412 项单测及 3 项 Provider 契约通过，3 个子进程辅助入口按设计忽略；沙箱内失败仅为 16 项测试禁止端口绑定。格式、差异检查通过，210 个受检文件均不超过 3000 行。完整原始验收范围仍未完成。
- 本批最终 workspace 全目标 Clippy（-D warnings）通过。

### 未变化快照中的失败验证（0.72.0-rc15，2026-09-06）

- 完成判定先检查当前快照的最新验证状态，再允许未变化工作区结束；测试失败不再被快照相同的提前返回掩盖，同一命令成功重跑可解除失败。
- 回归覆盖未变化且未测试、未变化但测试失败、同一检查成功重跑；版本同步 rc15。
- 审查 Harness 发现非 Git 工作区同样配置 Git 快照回调，返回 None 同时表示不支持与采集失败。本批未保留直接拒绝所有缺失基线的实现，避免普通问答回归；该项需要显式区分快照能力与错误，仍未完成。
- 中间实现完整 Core 415 项及 3 项 Provider 契约通过；移除缺失基线策略及其两个测试后重新执行针对性测试，不将中间统计作为最终完整门禁证据。
- 最终完成判定专项 3 项及 workspace 全目标 Clippy（-D warnings）通过；格式、差异及 210 文件行数检查通过。

### 快照能力与采集错误分离（0.72.0-rc16，2026-09-06）

- 增加可失败的验证快照回调：Ok(None) 明确表示不支持，Err 表示采集失败；既有 Option 回调作为兼容包装保留。初始错误以 VerificationSnapshot 错误在 Provider 请求之前返回，运行中错误拒绝完成。
- Harness 通过真实 Git 工作区发现区分普通目录、有效仓库和损坏元数据；仅明确的非仓库结果返回不支持，其余错误不会被 .ok() 吞掉。不向错误输出复制 Git 原始内容。
- 回归覆盖普通目录、未提交仓库、损坏 gitdir、已删除目录，以及 Core 在错误时零 Provider 调用、不支持时可正常回答。非 Git 写任务的结构化验收能力仍不等于完成，本批只消除能力缺失与运行错误的混淆。
- 版本同步 rc16；格式、差异、211 个手写文件行数检查通过。完整 workspace 最终测试已通过：CLI 375、脱离作业 13、Headless Runtime 15、Core 415、Provider 契约 3、Runtime Client 7、协议 31，共 859 项通过、5 个辅助入口忽略（按主测试组计数，不重复计算辅助子进程）。
- 最终 workspace 全目标 Clippy（-D warnings）、格式及差异检查通过；新增 VerificationSnapshot 的遥测分类为 verification_snapshot_failed。

### 验证快照绑定 HEAD（0.72.0-rc17，2026-09-06）

- 快照 ID 纳入已有 HEAD 提交字段，不同干净提交不再共享空 status/diff 指纹；同时覆盖初始无提交状态。没有新增内容扫描或 Git 子进程。
- 真实 Git 回归依次检查无提交、空提交、包含代码的第二次提交，确保 ID 不同；切回首次提交后恢复其 ID。Diff Review 组 11 项通过，包含原有大量未跟踪文件的有界扫描回归。
- 版本同步 rc17；211 个文件行数检查无超限。未跟踪文件仍依赖长度和 mtime，严格内容证据与扫描性能的兼容方案仍待实现；不以此批 HEAD 修复宣称快照验收全部完成。
- 本批 workspace 全目标 Clippy（-D warnings）、格式及差异检查通过。

### Core 独立保留验证证据（0.72.0-rc18，2026-09-06）

- ToolRegistry 默认建立内部验证记录回调，调用方无需订阅外部报告即可使用完成判定；配置外部回调仍只记录一次并通知订阅者。
- 前台验证命令的非超时 IO 错误记录 Failed，避免命令无法启动或捕获中断后沿用同一快照上的旧成功记录。超时保留原有 TimedOut 语义。
- 真实 Ruby 测试覆盖无报告回调下失败后成功，以及成功后删除工作目录导致启动失败、完成判定拒绝。工具组 51 项通过；211 个文件无超限，格式和差异检查通过。
- 本批最终完整 Core 417 项单测及 3 项 Provider 契约通过，3 个辅助入口忽略；workspace 全目标 Clippy（-D warnings）通过。原始整体范围仍未完成。

### Windows Job Object 首批（0.72.0-rc19，2026-09-06）

- Windows 共享命令路径创建非继承、kill-on-close Job Object，命令以 CREATE_SUSPENDED 启动，加入作业后通过所属线程句柄恢复；加入或恢复失败时不继续执行。取消和超时通过作业句柄释放清理后代，组长正常退出也先清理作业再等待输出流结束。
- 使用 windows-sys 公开 Win32 接口；新增 Windows 专用动态测试，覆盖组长退出和取消后已启动子进程不产生延迟写入。测试已交叉编译，未在本机 macOS 上执行。
- x86_64-pc-windows-gnu Core 含测试检查及 Clippy -D warnings 通过；Unix 共享执行 6 项回归通过；212 个受检文件无超限，版本同步 rc19。
- 原生 Windows 动态验证仍待完成；宿主在 CreateProcess 成功但 AssignProcessToJobObject 前被强杀仍有挂起进程遗留窗口，需进一步使用原子作业绑定或受保护启动器。不得将本批作为完整崩溃清理验收。
- 设计依据：https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects ；强杀窗口说明：https://devblogs.microsoft.com/oldnewthing/20230209-00/?p=107812 。
- 本机 workspace 全目标 Clippy（-D warnings）、格式及差异检查通过。

### 归档原子发布与文件类型检查（0.72.0-rc20，2026-09-06）

- 内容先写入私有随机临时文件并 sync，再通过不覆盖已有目标的硬链接发布最终内容地址；并发写者只读取完整已发布文件。清理临时文件后在 Unix 同步目录。文件系统不支持硬链接时显式返回归档错误。
- 读取只接受普通文件，Unix 打开使用 O_NOFOLLOW/O_NONBLOCK，Windows 使用 OPEN_REPARSE_POINT，并在打开后再次检查句柄对应文件类型；完整内容仍校验 SHA-256。
- 专项 3 项通过：四线程并发保存与读取、Unicode/工作区隔离重开、符号链接和 FIFO 拒绝；目录仅保留一个最终文件。版本同步 rc20，212 个受检文件无超限，格式和差异检查通过。
- 归档目录祖先的链接替换与崩溃后临时文件回收仍需加固，Windows 路径尚待原生验证；本批不代表完整存储隔离验收。
- 本批完整 Core 419 项单测及 3 项 Provider 契约通过，3 个辅助入口忽略；本机 workspace 全目标及 Windows Core 含测试 Clippy（-D warnings）均通过。

### Unix 归档目录句柄绑定（0.72.0-rc21，2026-09-06）

- 将归档文件操作抽到 Directory：Unix 以 O_DIRECTORY/O_NOFOLLOW 打开归档根和工作区目录，随后通过 openat/linkat/unlinkat 及同一目录句柄完成读写、发布、清理和同步，不在这些操作间重新解析工作区路径。
- 回归在打开句柄后将目录移走并替换为另一工作区目录的符号链接：新读写拒绝，已打开读取仍返回原内容，发布/清理只影响原目录，另一工作区不出现新文件。归档组 4 项通过。
- 版本同步 rc21；213 个受检文件无超限。归档根的祖先路径初始化、Windows 目录句柄实现及临时文件崩溃回收仍待处理，未将本批视为完整跨平台隔离验收。
- 上下文压缩组 8 项通过；本机 workspace 全目标、Windows Core 含测试 Clippy（-D warnings）、格式及差异检查均通过。

### Unix 归档祖先路径逐层打开（0.72.0-rc22，2026-09-06）

- 归档根从根目录句柄开始逐层 openat/mkdirat，不再调用沿路径隐式跟随链接的递归创建；新目录同步其父句柄，拒绝根配置中的父级遍历。
- 普通祖先链接被拒绝。为兼容 macOS 系统别名，仅对 UID 0 所有、位于 UID 0 所有且组/其他用户不可写目录中的链接允许跟随；威胁边界为无管理员权限的工作区进程。
- 新回归以用户可控制链接指向外部目录，确认保存失败且目标中没有创建任何文件；归档组 5 项通过，213 个受检文件无超限，版本同步 rc22。
- Windows 目录句柄保护与原生验证、崩溃后临时文件回收等仍在原始验收范围内。
- 上下文组 8 项、本机 workspace 全目标及 Windows Core 含测试 Clippy（-D warnings）、格式及差异检查通过。

### 真实 CLI 评测驱动首批（0.72.0-rc23，2026-09-06）

- 新增 scripts/agent_reliability_eval.rb 与说明文档，使用真实 CLI/config 在隔离临时 Git 仓库运行；当前覆盖 dirty_files 与 repeated_failure，另三类明确列入 pending，恢复率为 null。凭据和模型原文不进入报告。
- 样本执行红/绿预检，报告区分 prepared 与 evaluated，未执行不计成功。用量从本次状态目录读取，缺失保留 null；报告记录二进制 SHA-256。
- 驱动 3 项测试、10 个断言通过，覆盖真实子进程退出、超时回收、受保护文件缺失；两个样本红/绿预检通过，生成 target/agent-reliability-eval/report.json 和 report.md，实际 Provider 执行数为 0。
- 已构建 rc23 CLI。真实 Provider 调用被自动审批审查拒绝，理由为外部任务数据发送及费用尚未得到明确授权；没有通过其他路径重试。需要用户授权后才能执行此步骤。
- 216 个受检文件无超限，Ruby 语法、格式与差异检查通过。真实任务评测整项仍未完成。

### 评测结果采集加固（0.72.0-rc24，2026-09-06）

- 外部验收在独立目录使用固定清单与测试，仅复制待修实现，并限制运行期限；被改空的工作区测试不能骗过验收。
- 检查受保护文件内容、权限与新增配置；按 CLI 返回的精确会话读取合法用量，空输出、无效 JSON 和错误会话不作为有效结果。
- 在 rc25 工作区复验驱动：8 项测试、24 个断言通过；红/绿预检通过，实际 Provider 执行数仍为 0，另外三个场景尚未实现。

### 未跟踪文件验收内容指纹（0.72.0-rc25，2026-09-06）

- Diff 验收快照以固定 64 KiB 缓冲读取未跟踪文件内容，并计入权限；符号链接记录目标，非普通文件和读取失败拒绝生成快照。所有使用同一快照 ID 的验证、审查和撤销入口保持一致。
- 新回归确认等长替换后恢复原修改时间仍改变快照 ID，未变化内容保持稳定；链接目标变化可检测且不读取目标。Diff Review 组 13 项通过。
- 大量未跟踪文件仍省略展示行数统计，但验收内容必须读取；大工作区读盘开销尚需测量。工具归因本身仍使用弱元数据指纹，不能据此宣称内容级归因完成。
- 版本与变更记录同步；217 个受检文件无超过 3000 行的项，格式及差异检查通过。其余原始验收项仍保持未完成。
- CLI 全目标 Clippy（含依赖检查，-D warnings）通过。没有执行真实 Provider 调用，未提交、未推送。

### 流式检查点合并写入（0.72.0-rc26，2026-09-06）

- 连续文本不再逐增量重写完整历史：首段立即保存，后续按 250 毫秒定时或累计 16 KiB 保存；用量、工具执行边界及结束仍立即保存。静默 Provider 等待也运行刷新计时器。
- 取消 Future 时在释放执行租约之前刷新待写文本；刷新失败记录错误。增量保存错误和定时刷新错误均终止当前等待，不能继续执行该响应中的工具。
- 1000 个中文增量与 70 万字节历史的测试仅发生 3 次保存（初始边界、首段和末尾刷新）；字节阈值、用量、取消尾部、静默刷新及保存失败回归通过。
- 检查点筛选测试 21 项通过、1 个跨进程辅助入口按设计忽略；最终流式事件组 4 项通过（与筛选组部分重叠）。218 个受检文件无超限。
- 这是合并完整快照写入，尚非增量日志存储。强杀进程或断电可能丢失未刷新的文本尾部；该延迟不应用于工具副作用边界。长会话真实磁盘吞吐仍待测量，不能以调用次数测试宣称整体性能验收完成。
- 最终 workspace 全目标 Clippy（-D warnings）、格式及差异检查通过；版本元数据一致。未调用真实 Provider，未提交或推送。

### 验证失败不随报告淘汰（0.72.0-rc27，2026-09-06）

- 完成判定使用按快照和命令索引的最新状态，与最多 256 条的详细报告分开；相同命令重复执行覆盖状态，不累积重复摘要。两个集合在同一锁内更新。
- 新回归确认 276 条后续成功不能清除先前失败，其他快照的通过不能解除当前失败，原命令在原快照重验成功才可通过；1000 次重复记录只保留一个验收状态。
- 验证筛选 11 项通过；完整 Core 429 项通过、3 个辅助入口按设计忽略，三协议本机 Provider 契约 3 项通过，没有调用真实 Provider。
- 218 个受检文件无超限；版本同步 rc27。该状态索引仍属于当前执行器内存，任务级验收契约和跨重启验证证据恢复仍待完成，不能据此勾选完成与进展整项。
- workspace 全目标 Clippy（-D warnings）、格式及差异检查通过。未提交、未推送。

### 验收状态随检查点恢复（0.72.0-rc28，2026-09-06）

- CheckpointMetadata 增加兼容旧数据的 verification_evidence 字段，只保存快照、命令及最新状态，不保存输出摘要。未完成 Session 的执行器初始化会读回记录，完成 Session 不注入旧任务证据。
- 消息边界和结束时保存状态；取消导致 Recorder 释放时补存报告后产生的状态变化，并在释放执行租约前落盘。恢复旧证据不会覆盖同一执行器已收到的新结果。
- 磁盘保存与重建执行器用例确认失败恢复、重验成功更新及完成任务隔离；验证筛选 13 项通过。版本同步 rc28，218 个受检文件无超限。
- 这里验证的是执行器重建与真实文件恢复，不是完整 Runtime 派工重建。任务级验收契约、外部副作用核对和硬崩溃窗口仍需继续验收。
- rc28 完整 workspace 回归通过：CLI 378、脱离作业 13、Runtime 集成 15、Core 431、Provider 契约 3、Runtime Client 7、Protocol 31，共 878 项通过；5 个子进程辅助入口按设计忽略，不重复计入辅助进程自身输出。
- workspace 全目标 Clippy（-D warnings）、格式和差异检查通过。实际外部 Provider 执行仍为 0；未提交或推送，完整目标保持未完成。

### 完成验证反馈可操作化（0.72.0-rc29，2026-09-06）

- 完成检查一次采集快照并返回具体反馈，区分快照不可读取、快照能力丢失、当前改动缺少验证和当前命令失败。Agent 下一轮收到对应原因。
- 失败反馈只读取当前快照，最多展示 8 条、每条命令 1024 字节，并标明截断和省略数量；命令作为 JSON 记录数据传入，不授予执行权限。
- 验证相关 14 项回归通过，包含真实 Agent 收到缺少验证的明确原因及超长中文命令的有界反馈；218 个受检文件无超限。版本同步 rc29，完整 workspace 878 项回归的最近基线仍为 rc28。
- 该反馈辅助模型处理已知失败，不替代用户任务级验收契约；其余未完成范围保持不变。
- workspace 全目标 Clippy（-D warnings）、格式与差异检查通过；未提交或推送，未执行外部 Provider 评测。

### 跨快照失败必须重验（0.72.0-rc30，2026-09-06）

- 完成门禁查找当前执行器及恢复证据中的已知失败；对应命令没有在当前快照通过时继续阻止完成，不因代码快照变化而丢弃失败。
- 反馈将历史失败标记为需要在当前快照运行；当前检查失败仍显示当前状态。旧快照成功记录不会误列为失败。
- 新回归覆盖旧快照失败、当前快照无关测试通过、执行器证据恢复与原失败命令重验通过。验证筛选 15 项通过；218 个受检文件无超限，格式与差异检查通过，版本同步 rc30。
- 此约束覆盖已知失败，尚不能证明从未执行的用户验收项已满足；任务级验收契约及其余未完成范围继续保留。
- workspace 全目标 Clippy（-D warnings）通过。未执行外部 Provider 评测，未提交或推送。

### 显式命令级验收要求（0.72.0-rc31，2026-09-06）

- 配置新增可选 agent.verification_commands，由共用 Harness 接入；最多 32 条、每条 2048 字节，仅接受非敏感且可识别的验证命令。清单从第一轮提供给 Agent，所有条目须在当前 Git 快照通过。默认空清单保持普通问答行为。
- 清单保存在执行检查点，未完成任务恢复时合并原要求，不能通过删配置削弱；合并超限拒绝启动。配置不自动执行命令，不扩大审批或沙箱权限。
- 拒绝未引用换行串联，防止后一条成功掩盖前一条失败。命令检查不能保证所有框架特殊参数都被识别，文档明确这一边界。
- Core 验证组 18 项、CLI 配置组 15 项通过，覆盖初始提示、部分清单阻止完成、非 Git 拒绝绑定、输入原子验证、保存恢复与已完成任务隔离。新增 docs/TASK_VERIFICATION.md 和配置示例。
- 219 个受检文件无超限。自然语言验收覆盖、完整 Runtime 重建、未完成评测场景与真实 Provider 评测仍在原范围内；本批不将全目标标为完成。
- 补充检查点筛选 21 项通过、1 个辅助入口忽略；Agent/子 Agent 筛选 69 项通过（筛选组有重叠）。workspace 全目标 Clippy（-D warnings）、格式及差异检查通过；未调用真实 Provider，未提交或推送。

### 源码尺寸验收复核（0.72.0-rc32，2026-09-06）

- 当前脚本检查 219 个手写文件，无超过 3000 行的项；CI 已运行 scripts/check_source_size.rb。工程拆分这一独立验收项标记通过，不代表其余七类可靠性要求完成。
- 超过 2000 行的九个文件已评估：

| 文件 | 行数 | 职责边界决定 |
| --- | ---: | --- |
| daemon.rs | 2924 | TaskManager、传输和存储已分模块；保留守护进程装配入口，新增职责须进子模块。 |
| tools.rs | 2917 | 后台 Shell、验证与测试已抽出；工具定义和注册暂保持统一，新增执行逻辑进入子模块。 |
| tui.rs | 2679 | 状态、事件循环、渲染及命令分模块；保留界面装配。 |
| daemon/diff_review.rs | 2566 | 当前仍是同一 Git 审查域，暂保持 API 与快照操作一致；未来拆分可沿 API 与快照采集分界。 |
| web.rs | 2437 | 保留 Web 桥接装配及路由；后续扩展优先拆出 handler，避免继续集中。 |
| main.rs | 2368 | 保留 CLI 参数与入口分派，业务已进入 Harness 等模块。 |
| tests/headless_runtime.rs | 2258 | 同一真实进程测试夹具；未超限，后续增长时可沿 support/ 用例拆分。 |
| daemon/session_store.rs | 2179 | 持久会话操作保持单一存储职责，测试已抽出。 |
| daemon/control_api.rs | 2046 | 保留统一受权控制 API 的路由与处理边界，暂不为行数拆散权限检查。 |

### 真实 CLI/Runtime 验收清单恢复（0.72.0-rc32，2026-09-06）

- 使用隔离目录、真实 CLI 与本机模拟 Provider，模型仅返回完成文本、不执行检查：首轮三次响应后 CLI 返回 partial/unverified、退出码 5，Runtime Turn 同为 partial，Core 检查点保留必验命令。
- 实际停止 Daemon 并删除配置中的清单，再用同一 Session 恢复：仍返回 unverified/退出码 5；新建独立 Session 不继承旧清单，正常返回 completed。共七次本机模拟请求，无外部 Provider 调用。
- 集成测试 1 项通过，用时 7.49 秒；这是根任务验收清单的真实重启验证，不证明 Worker 派工目录或外部副作用恢复已完成。未提交、未推送。
- workspace 全目标 Clippy（-D warnings）、格式和差异检查通过。

### Web 部分完成恢复入口（0.72.0-rc33，2026-09-06）

- Web 侧栏补齐 partial 后台 Agent 的重试与换模型重试按钮；后端原已允许该状态，当前修改只补 UI 入口，沿用现有 i18n 文案。
- yarn lint 与 yarn build 通过（TypeScript 与 Vite）；构建仍提示主 JS 超过 500 kB，未将该提示隐藏。
- 实际浏览器加载真实 RuntimeSidebar 组件和固定 partial 数据，展开后确认中文状态与两按钮可见；点击分别产生 qa-partial-worker/retry 与 qa-partial-worker/retry_model 回调。该证据证明组件渲染和事件绑定，不替代真实 Worker 重试或模型切换的端到端验收。
- 临时浏览器页、Vite 进程和夹具文件已清理；无真实 Runtime/Provider 副作用。版本同步 rc33，未提交或推送。

### 子 Agent 重试等待事件（0.72.0-rc34，2026-09-06）

- ChildEventSink 将 Provider RetryWait 映射为带稳定子 Agent ID、尝试次数与等待时长的专用事件；子任务正文仍不混入根任务文本流。
- CLI 文本及 JSON、本地 TUI、Runtime TUI 与 Web 状态条接入；Web 只投影允许字段并使用现有三语言服务端文案机制。
- Core 转发专项与 Web 事件投影专项通过，yarn lint/build 通过；主 JS 仍有 500 kB 警告。CLI 子任务筛选 5 项通过，包含 Runtime 日志重开后的公开事件保留、TUI 活动展示与 Web 字段投影；workspace 全目标 Clippy（-D warnings）通过。219 个受检文件无超限，格式及差异检查通过。
- 此批覆盖事件传递与活动展示；Agent 列表持久阶段、真实限流到浏览器渲染端到端、Windows 原生验证与其余原始清单仍待完成。未调用外部 Provider，未提交、未推送。
- 子任务筛选额外命中的真实 Runtime 派工/等待集成初次因沙箱禁止本地 bind 失败；在允许本机端口的环境按原测试重跑通过（4.85 秒），模拟 Provider 不访问外网。此既有集成证明派工等待未回归，不作为新重试事件的全链路验收。

### Runtime 持久等待阶段（0.72.0-rc35，2026-09-06）

- 公共协议与内部 Agent 增加向后兼容的可选 retry_wait（尝试次数、等待毫秒数）；根任务和子任务独立记录，列表与详情投影均保留，不改变生命周期枚举和停止权限。
- Provider 等待结束发出 RetryStarted，主/子事件路径明确清除等待；新轮次、工具开始、终止和 Runtime 重启也清除，终态拒绝迟到的等待事件。
- 本机模拟 HTTP 回归验证 RetryWait/RetryStarted 顺序与 Retry-After 下限；存储专项验证真实文件保存、状态边界及旧 JSON 兼容。Web lint/build 通过；主 bundle 仍提示超过 500 kB。新增 TUI 列表/详情实际终端缓冲区与存储终态回归共 2 项通过；Core 子事件转发回归通过，workspace 全目标 Clippy（-D warnings）、格式及差异检查通过。219 个受检文件无超过 3000 行的项。
- 尚需真实 Runtime HTTP/API 到浏览器刷新后的全链路等待验收，其他原始验收项仍保持未完成；本批未访问外部 Provider、未提交或推送。

### 真实 Runtime 重试状态集成（0.72.0-rc36，2026-09-06）

- 子 Agent 真实集成改用本机 Provider：根任务等待用户输入，子任务首次响应 429/Retry-After 2 秒；公共 agent.list 可观察等待，父任务没有等待标记。第二次请求以测试屏障保持，agent.get 确认 Running 且 retry_wait 清空后才返回最终正文。最终 Completed 无等待残留，Provider 共三次请求。
- 该集成专项通过（5.70 秒），复用真实 CLI、Runtime、公共 Rust Client 与 Worker 持久化链路；补充主任务相同中间状态验收。版本同步 rc36。
- 完整 workspace 回归通过：CLI 382、脱离作业 13、真实 Runtime 17、Core 437、Provider 契约 3、Runtime Client 7、协议 31，共 890 项通过，5 个辅助入口忽略（辅助入口由相关进程测试实际调用）；主任务重试状态新增用例通过。全目标 Clippy（-D warnings）、格式及差异检查通过；Web 构建通过，主 JS 大包警告保留。219 个受检文件无超限。浏览器实际刷新链路和其余原始清单仍待验收，未调用外部 Provider、未提交或推送。

### 普通文件工具窗口归因（0.72.0-rc37，2026-09-06）

- 红测复现：已脏 tracked.txt 等长改写并恢复 mtime 后，整体快照变化，但旧长度/mtime 归因指纹不变。
- 普通文件改用已有流式内容指纹；符号链接比较链接本身，Git 索引一次批量读取并纳入各路径对象及阶段信息。只有 NotFound 作为缺失状态，其余读取错误上报，避免把无法读取当成删除。
- 移除归因指纹中的二进制/行数展示统计，跨未跟踪文件统计阈值不再影响未改动文件；补充保留 mtime 的 tracked/untracked 改写、工作区内容不变但索引变化，以及跨阈值未改动路径回归。
- 最终 Diff/归因回归 15 项通过，包含追加索引与跨阈值用例；219 个受检文件无超限。归因置信度仍是 ToolWindow，不能证明并发窗口中的唯一作者；Gitlink 仅比较索引及嵌套 HEAD，不将嵌套脏内容归因或大型工作区 IO 性能视为已验证。
- 版本同步 rc37；未访问外部 Provider，未提交、未推送。
- Runtime 子 Agent 工具事件归因专项通过（0.85 秒）；workspace 全目标 Clippy（-D warnings）、格式与差异检查通过。本批没有重复完整 workspace，最新全量仍为 rc36 的 890 项通过。

### 子任务集成评测样本（0.72.0-rc38，2026-09-06）

- 评测驱动增加 subtask_integration：要求前台只读 scout 先完成诊断、根任务修复 src/lib.rs 并通过独立验收；全部其他文件保持不变。根任务未派工、子任务未完成或仅正文声称完成都失败。
- 此场景读取真实 CLI NDJSON，要求同 ID 且有序的子 Agent started/completed，profile=scout、background=false；最终记录必须完整，损坏/截断日志不算完成。外部验证与防篡改沿用独立目录测试。
- Ruby 10 项测试、37 个断言通过；三个样本红/绿预检通过，实际 Provider 执行数仍为 0，待实现场景为 interruption_recovery 和 compression_constraints。真实外部评测仍待用户授权，不绕过此前审批拒绝。
- 新增本机模拟 Provider 的真实 CLI NDJSON 派工集成，正在验证输出兼容性；不将该测试计为真实外部 Agent 评测。版本同步 rc38，未提交、未推送。
- 真实 CLI 验证发现并修复新建 run --local 会话未保存就获取执行所有权导致 session not found；只预先保存本次新分配 ID，恢复会话仍在持锁后刷新，避免旧快照覆盖。初次红测在模型请求前失败，修复后派工 NDJSON 专项通过（2.85 秒）。补充本地同会话续跑回归，并执行完整 Runtime 集成组。
- 最终真实 Runtime/本地 CLI 集成组 18 项通过（36.37 秒），包含新本地 NDJSON 派工及相同 Session 续跑；全目标 Clippy（-D warnings）、格式和差异检查通过。219 个受检文件无超限，最新完整 workspace 仍为 rc36 的 890 项通过，本批不冒称已重跑全量。

### 显式压缩后的约束评测（0.72.0-rc39，2026-09-06）

- 增加 compression_constraints 样本：固定会话首条 User 保存原始修复与文件约束，十条历史 Assistant 构成待压缩内容；使用已有 Web 桥接 CLI 显式 /compress，再以同 Session 续跑，后续提示不重复原约束。
- 要求压缩完成事件、持久检查点代次、消息数量减少、原始 User 约束逐字保留，以及最终独立修复验收和文件保护；两阶段共享超时，手工压缩用量不可完整观测时总 Token 保留 null。此项不替代自动压缩阈值评测。
- Ruby 12 项测试、44 个断言通过；四个样本红/绿预检通过，实际执行数仍为 0，待实现 interruption_recovery。新增真实 CLI 固定会话压缩/续跑集成正在验证；未联系外部 Provider。
- 版本同步 rc39，未提交、未推送；完整七项验收仍未完成。
- 真实 CLI 桥接压缩/同会话续跑专项通过（3.28 秒）：最小 JSON 会话夹具可加载，generation=1、消息数减少、原始 User 约束逐字保留，模拟 Provider 恰好两次请求。此证据验证驱动所用入口，不作为真实外部评测成功计数。
- workspace 全目标 Clippy（-D warnings）、格式与差异检查通过，219 个受检文件无超限。最新完整 Runtime 组仍为 rc38 的 18 项通过，完整 workspace 仍为 rc36 的 890 项通过，未将专项混作全量。

### 中断注入前的评测进程控制（0.72.0-rc40，2026-09-06）

- 移除 Open3 自动等待线程；独立 Ruby 监督器保持组长存活，通过私有状态管道传递实际命令退出码，驱动先清理组再回收组长，避免超时与提前回收的 PID 竞态。
- 正常退出也清理仍占用 stdout/stderr 的同组后代；驱动被强杀时存活管道 EOF 触发监督器清理。新增可选中断观察器，返回值区分注入中断和超时；观察器异常仍走清理。
- 清理边界是 Unix 进程组，主动 setsid/新建进程组不在该保证内；中断恢复评测场景尚未实现，本批不填恢复成功率。版本同步 rc40，未访问外部 Provider、未提交或推送。
- Ruby 16 项测试、56 个断言通过；四个样本不联网红/绿预检通过，实际评测执行数为 0，恢复率仍为 null。Ruby 语法、差异及源码尺寸检查通过，220 个受检文件无超限。本批仅改评测 Ruby 与版本文档，没有重跑 Rust 全量。
