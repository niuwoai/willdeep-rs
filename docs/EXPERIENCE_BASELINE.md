# 体验基线清单：终端 coding agent 的「手感」

> 基线对象：Claude Code / Codex CLI 的日常交互。目标不是抄界面，而是每一项都有等价物。
> 状态列按代码实况维护，改一项更新一项；验收以 `crates/willdeep-cli/src/tui/test_suite/` 的事件级测试为准。
> 最后更新：2026-09-21 @ v0.78.0-rc30。

| # | 能力 | TUI | Web | 备注 |
|---|---|---|---|---|
| 1 | 开工前一句话说打算怎么做，阶段切换一句话汇报 | 已到位（rc6 提示词） | 已到位 | 靠模型听话；人话率进评测（ADR 第 2 项） |
| 2 | 模型中途文字实时进聊天区 | 已到位（rc4） | 已到位 | |
| 3 | 每次工具调用一行、完成后原地改标记 | 已到位（rc4） | 已到位（逐轮工具轨迹） | |
| 4 | 连续工具行折叠，最新进展永远可见 | 已到位（rc5/rc10） | 已到位（rc17） | Web 运行卡片按同一规则分轮折叠，外层再按 5 行兜底 |
| 5 | 思考型模型的思维链可见、不占地方、正文一来就隐藏 | 已到位（rc6/rc9） | 已到位（rc16） | Web 在运行卡片下方画灰色尾巴行（最后 240 字），正文或工具一来即清 |
| 6 | 轮次结束有明确信号（分隔线 + 铃） | 已到位（rc7） | 已到位（rc18，仅分隔线） | 浏览器不响铃；恢复的轮次不知道开始时间，只报工具数 |
| 7 | 输入框常驻「轮到谁」状态 | 已到位（rc7） | 已到位（rc18） | 输入框下方提示：跑时「本轮进行中 · 回车即送达」，空闲「轮到你」 |
| 8 | 本轮进行中插话直接送达，不打断手上的活 | 已到位（rc11） | 已到位（rc15） | Web 没有本地队列：送不到时提示稍后再发，不暗中排队 |
| 9 | Esc 中止当前轮，队列随即发出 | 已到位 | 已到位（停止生成） | |
| 10 | 计划 / 待办可视化 | 部分（`/plan` 计划卡片） | 部分 | 无独立 todo 工具；模型自报计划，宿主持久化步骤状态 |
| 11 | 检查点回退（类似 `/rewind`） | 已到位（rc28，`/rewind`） | 已到位（rc28，↶ 对话框） | 每轮开始前拍工作树快照进私有影子 git 仓库；`session.rewind` 原地截断对话、可选恢复文件，被覆盖的原件进回收区；见 `docs/CHECKPOINT_REWIND.md` |
| 12 | diff review 一键回滚 | 已到位（`/diff`） | 已到位 | |
| 13 | 审批弹回发起端，跨端不重复弹 | 已到位（rc2） | 已到位 | |
| 14 | 审批档位随时可切、对本轮立即生效 | 已到位（rc1） | 部分（只读会话档位） | Xedit 不调用 `session.update_approval_mode` |
| 15 | 终端残影 / 控制字符安全 | 已到位（rc9） | 不适用 | |
| 16 | 首次使用零配置可跑 | 已到位（rc19） | 已到位（rc19） | 环境里有 `SOMEIM_API_KEY` / `ANTHROPIC_API_KEY` 就直接跑，不逼首次设置；旧写法在开屏、`config check`、`doctor` 里点名 |
| 17 | 长任务不因轮次上限中途判失败 | 已到位（触顶交出部分结果，缺省 200 轮） | 已到位 | token 预算闸门见 rc13 |
| 18 | 会话重开后完整回放（含工具行） | 已到位（rc20） | 已到位（rc20） | 投影带每条调用的名字、脱敏摘要与有无结果；回放不知道成败，只画 ✓ / … |
| 19 | 轮次结束后预测下一句：空输入框灰字、Tab 采用 | 已到位（TUI rc29 · Web rc30） | 已到位（Xedit 1.385.0-rc1） | 与标题同一档模型的独立小请求，只填入不发送，晚到按世代号丢弃；Web 走独立端点，刷新即无 |

## 验收方式

- 每项「已到位」至少对应一条事件级测试；改动行为时同步改测试。
- 整轮回放测试：喂一段真实的 Runtime 事件流（`~/.willdeep/runtime/events.ndjson` 脱敏片段），断言最终聊天区行序与状态。
- Web 列的 4、5、6、7、8 项已于 rc15 至 rc18 到位，第 16 项 rc19、第 18 项 rc20、第 11 项 rc28 到位；剩下的是第 10 项（计划 / 待办）与第 14 项 Web 端的「部分」。
